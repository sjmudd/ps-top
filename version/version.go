// Package version returns the version of the application(s)
package version

const (
	version = "0.7.9"
)

// Version returns the current application version
func Version() string {
	return version
}
